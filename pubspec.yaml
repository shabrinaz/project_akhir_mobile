name: project_akhir_mobile
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.3.3 # Untuk database SQLite
  path_provider: ^2.1.3 # Untuk menemukan lokasi penyimpanan database
  shared_preferences: ^2.2.3 # Untuk menyimpan status login/session
  crypto: ^3.0.3 # Untuk hashing password (penting!)
  http: ^1.2.1
  intl: ^0.18.1
  url_launcher: ^6.2.2 
  geolocator: ^11.0.0
  flutter_map: ^6.1.0
  latlong2: ^0.9.1
  flutter_local_notifications: ^17.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  image_picker: ^1.1.2

flutter:
  uses-material-design: true
  assets:
    - lib/assets/image/
